# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production
.dev.vars

# password-gated files — never commit these, uploaded to R2 directly instead
.private/

# resume PDFs contain personal phone/email — excluded until Phase 5b's real
# password protection is built (see ROADMAP.md, Phase 5b)
public/resume/

# wrangler local dev state (local R2/KV emulation)
.wrangler/

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
